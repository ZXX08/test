# Model Weights

Final full fine-tuning checkpoints are saved under:

```text
models/qwen3-4b/full/sft/
```

This directory is intentionally not committed because model weights are large. Download or copy a completed checkpoint into this path before running prediction or inference.
